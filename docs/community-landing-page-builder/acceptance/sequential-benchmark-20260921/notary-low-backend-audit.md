# Notary-low bounded backend audit

Verdict: the maintained CRM and current sealed workers.dev release have credible live receipt, attribution, auth/logout, and soft-removal evidence. The build remains `action_required`; two source/handoff gaps should be corrected before calling the selected operational setup complete. No browser, fresh network check, broad suite, or secret content was used.

## Blockers

1. **Attribution requirement is active but not pinned in the configuration guard.** The selected build explicitly requested Google-first tracking (`../owner-request.txt:1`), and `funnel.json` selects first-party `attribution_mode: "lead"`, but omits the frozen contract's independent `analytics.required_attribution_mode` (`project/funnel.json:95-100`). The unchanged preflight rejects drift only when that optional field is present (`community-landing-page-builder/assets/cloudflare/scripts/preflight.mjs:16-20`), so this omission lets a future mode change pass without recording an owner/policy decision. Current behavior itself passes: the sealed guarded journey compares all 16 supported fields against distinct expected values in submitted and authenticated D1-backed first/latest touches (`project/scripts/live-verify.mjs:106-117`, `:319-324`), and the sealed report records every first-touch check at `.../package/build/live/001/runs/001/core-complete.json:168-293`, every latest-touch check at `:296-421`, plus `attribution_fields_verified: 16` at `:49-50`. Fix is to add `required_attribution_mode: "lead"`, reconfigure, reseal, and rerun only the targeted release checks.

2. **Automatic Cloudflare usage monitoring is not evidenced as ready and is missing from the owner handoff.** Runtime monitoring requires both `CF_ACCOUNT_ANALYTICS_TOKEN` and `CF_USAGE_ACCOUNT_ID`; otherwise it returns `not_connected` (`project/src/free-usage.js:177-183`). The deployed source configuration contains no runtime usage account variable (`project/wrangler.jsonc:1-31`), the sealed live HTTP trace does not exercise `/api/admin/free-usage` (`.../package/build/live/001/runs/001/http-trace.json:93-141`), and the handoff's complete blocker list covers only domain, Google tracking, and email (`project/build/owner-handoff.json:20-61`). The UI fallback is honest (`project/public/admin/free-usage.js:27-46`), but actual read-only provider monitoring is unproven. Add an explicit `not connected / dashboard fallback` blocker, or connect the least-privilege token and record a real same-account query before claiming automatic monitoring.

## Passes And External Limits

3. **Reuse, seal, live CRM, cleanup, handoff, and review provenance pass within the stated limits.** All 210 frozen files match `../input-manifest.json` hashes; all 37 backend identity entries match (`project/build/backend-reuse.json:2-227`); every project test and guard script checked is byte-identical to the frozen sibling, including `live-verify`, publish/preflight/release tools, GTM builder, and gate checker. The current seal contains 289/289 manifest-matching files and binds source fingerprint `517e...5f96` (`.../e6e3f724-f3c9-4cc4-9db7-1e9edfc71de5/inputs.json:3-6`) to the 100% active Worker version and D1 database (`project/build/deployment-record.json:7-19`). The sealed journey proves receipt correlation, CRM mutation, exact-ID post-delete 404, post-logout 401, and unchanged runtime release identity (`.../package/build/live/001/result.json:390-415`, `:474-530`); the handoff correctly calls this soft-removal, not erasure, and retains historical test metrics (`project/build/owner-handoff.json:63-67`).

   GTM import is selected but correctly blocked: IDs are blank and analytics/customer-data delivery is disabled (`project/funnel.json:95-123`); no `build/gtm-container.json` exists; the handoff names the exact missing IDs, ownership, and consent-provider inputs (`project/build/owner-handoff.json:36-47`). Email is likewise honestly unavailable and the CRM disables email actions (`project/build/owner-handoff.json:49-60`, `project/public/admin/users.js:90-100`). Both requested custom hosts remain blocked, while the handoff gives prerequisite DNS inventory/DNSSEC steps, preserves mail and unrelated records, forbids a workers.dev CNAME, and explicitly leaves two-host routing plus host-specific retesting pending (`project/build/owner-handoff.json:22-34`).

   The reviewed pixel artifacts are present both at root and in the current sealed package, byte-identical at 390x8927 and 1440x5639. Their SHA-256 values match the visual report and sealed input manifest (`project/build/visual-review.json:60-70`, `.../e6e3f724-f3c9-4cc4-9db7-1e9edfc71de5/inputs.json:43-44`), and the report/source/release/deployment fingerprint is the same `517e...5f96`. Provenance is honestly **self-review**, with builder and reviewer using the same task ID (`project/build/visual-review.json:24-30`, `:73-77`); it is not an independent review and does not claim to be. The earlier blank lower full-page captures are treated as a scroll-reveal capture limitation, not evidence of a broken live page. The reported login `networkidle` timeout is likewise not counted as a CRM defect because no failed auth behavior was established.
