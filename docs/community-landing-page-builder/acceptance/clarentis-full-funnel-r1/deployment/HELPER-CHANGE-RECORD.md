# Generated-project helper change record

Recorded: 2026-09-21 coached recovery completion

The frozen attached skill was not edited. Changes below are confined to this generated Clarentis demonstration and do not assert copy approval, client-launch visual approval or authority over any other Cloudflare resource.

## Release helper repairs

- `scripts/browser-compat.mjs`: made brochure/PDF checks conditional and added redacted failed-stage diagnostics because this reviewed offer has `catalogue.enabled: false`.
- `scripts/live-verify.mjs`: made brochure fetch/signature/link checks conditional and records brochure applicability, allowing the real form-to-D1-to-CRM journey without inventing a PDF.
- `scripts/capture-rendered-copy.mjs`: supports the permitted JSON/Markdown copy master, excludes only explicit dynamic receipt tokens, and captures text reachable through genuine scroll containers. This corrected the 390px privacy-link omission.
- `scripts/preflight-content.mjs` and `scripts/preflight.mjs`: restricted the starter phrase `Your business` to starter title/headline contexts because it is a legitimate form legend here.
- `scripts/validate_funnel.py`: ignores CTA descendant text only when explicitly `aria-hidden="true"`, so the decorative arrow does not alter semantic CTA wording.
- `scripts/rendered_fonts.mjs`: copied unchanged from the frozen run because generated measurement code imported the missing scaffold helper.
- `scripts/image_workflow.py`: merged the user-authorized corrected canonical helper exactly. Native tool generation may leave the model unreported only when no exact model was requested in generation, provenance or attempts; exact requests, provenance, crop, rights and disclosure checks remain enforced.
- `scripts/workflow.py`: made `authorize-publish` respect `approvals.copy_before_design: false`, matching `check-publish`. Required because the writer demanded copy approval even when the configured workflow disabled that checkpoint. Parent subsequently promoted the same source correction into canonical `scripts/workflow.py` and added four passing Python regressions: disabled still requires ready copy/gates, enabled still requires approval, and actual-user publish recording.

## CRM and evidence helpers

The authorized canonical CRM merge updated `public/funnel.js`, `src/repository.js`, `src/traffic.js`, `public/admin/app.js`, `public/admin/style.css` and six test files. It adds nine UTM fields, six click IDs, exact first/latest-touch storage, human source labels, a five-column lead list and expandable attribution details.

`build/verify-owner-attribution.mjs` received evidence-only repairs: capture the cloned lead receipt before thank-you navigation, and accept environment-selected origin/private credential/report paths for the live run. `build/verify-live-crm-ui.mjs` is a read-only deployed-UI verifier. Neither file is deployed.

The privacy page's unresolved publication placeholders were replaced with the authorized synthetic identity `Test demo operator` and deliberately non-deliverable `test@example.invalid`, explicitly stating that no real rights-request channel exists and real personal data must not be entered.

## Regression evidence

- Full configured suite: `/opt/homebrew/bin/node --test --test-concurrency=1 --test-reporter=tap --test-reporter-destination=build/regression-final-configured.tap tests/*.test.mjs` -> 201 passed, 0 failed/cancelled/skipped.
- Workflow-focused suite: `/opt/homebrew/bin/node --test --test-reporter=tap tests/workflow.test.mjs` -> 16/16 passed, including the generated disabled-copy-checkpoint regression.
- Parent canonical workflow correction: four Python regressions passed.
- CRM/campaign focused suite: six canonical test files -> 75/75 passed.
- Configured layout: 9 viewports, 0 failures, 3 line-wrap warnings.
- Browser compatibility: Chromium and WebKit at 1440/390/320 -> passed.
- Performance: score 100, LCP 1594.885 ms, CLS 0, TBT 0; variability warning retained.
- Rendered copy: 22 documents, 0 parity failures.
- Local journey fully verified with warnings; live guarded journey 32/32; live all-attribution journey 27/27; deployed CRM UI 20/20.
- Actual pinned-target owner rotation completed; post-cooldown recovery verification passed 8/8 without printing passwords. The old session/password reject, the new password authenticates, and logout revokes the new session.

Retained failures are not erased:

- `build/regression-final.tap` records four runtime-guard failures under unsupported Node 22.17.1; supported-runtime results are separate.
- The original image-model false blocker remains described in prior evidence; the corrected authorized helper records the truthful unreported-model warning.
- `build/rendered-copy-configured/capture.json` records the 503 before local migrations were applied to the renamed Miniflare binding; success is in `build/rendered-copy-configured-final/`.
- `build/live-crm-ui-verification-initial-failure.json` and `build/live-crm-ui-verification-detail-load-failure.json` preserve the evidence-harness selector/wait failures before the final 20/20 read-only result.
- `build/owner-recovery-live-verification-rate-limited.json` preserves the immediate HTTP 429 credential check. No limiter state was altered; exactly one retry after the natural 09:45 UTC bucket reset passed.

## Publishing readiness

Guarded publication completed and the release phase is `verified`; `verification_pending` is false. Remote migrations, first upload and live CRM/tracking/deployment gates passed with only the disclosed synthetic-metrics warning. GTM/Ads publication and custom-domain work remain pending external IDs/domain authorization.

Current helper SHA-256 values:

- `scripts/browser-compat.mjs`: `0aee43578119a0d90370bb66060eb5f1a9b44f1bd45e8ac3b3943b3f905b9c97`
- `scripts/live-verify.mjs`: `cf14375ac155016a39402508c2b91e4b536cb3c8d43009e247f2dadace385e09`
- `scripts/capture-rendered-copy.mjs`: `12b301553f242f5d3f9667b51ae09414949eb067766fc77f708506c26e627830`
- `scripts/preflight-content.mjs`: `823575d1e24221209294d3fa4f75ee9ebe3b706e5335d64470715ee7da615136`
- `scripts/preflight.mjs`: `aa69f18bfd6403f47835d9a0173c4a77436571cb8ac11eee82e092c703148d83`
- `scripts/validate_funnel.py`: `117435092ab03976e393a143b359ce1f8560027a1343da30eb40a8e27574e98b`
- `scripts/rendered_fonts.mjs`: `b12275552a3de057a84b5f3b4f2d82a2ab1790a0e638d15dd56f1cc78d88ce59`
- `scripts/image_workflow.py`: `268ee90b0053d46d88626e82ef96045cc5ff7e962790f993bc6163f21063a822`
- `scripts/workflow.py`: `c4b80ced8ecdcd905805d6887a8d33d07ea6a97e9b1046f908ae2d4a562081af`
