# External-reference attempt 3: accepted local result

**Result: local acceptance passed for business 2 on 2026-09-20.** No confirmed blocker remains in the inspected page-only scope. Preview: http://127.0.0.1:4175/ . No parent repair was made to the generated page. No CRM, Cloudflare connection, live lead or deployment occurred.

## Independent run

- Business: https://gardenroomco.com/ . External presentation example: https://www.greenretreats.co.uk/ . Same plain owner request as attempts 1 and 2; only run paths changed.
- Builder Hubble, `01a0bec3-01a7-77e1-8e7b-8cf67071e348`, gpt-5.6-sol xhigh, fork_context=false. Started 15:20 Kyiv, finished about 15:48. Empty project verified. No parent conversation, prior page or audit feedback supplied.
- Source `d8163be`, frozen 175 files. Before/after manifest SHA-256 is identical: `6cd9eb20d2d0910c556cfd7c75ef324461dbf8bc8fbdbff16790ea3558be4621`.
- Available execution record: 91 command entries, no earlier-run/audit/history reads found. The only app tool call opened the local preview. Installed Playwright was used as runtime infrastructure. Fresh context and instructed directory boundaries are not OS sandboxing or proof of a newly provisioned ChatGPT account.
- Reviewer Russell, `01a0bedc-dbea-7bb2-b394-30e0c85c667e`, same model/effort and fork_context=false. Received only current skill, final raw artifacts, business/reference URLs and working browser infrastructure. No builder QA conclusions or previous findings supplied. See `evidence/external-reference-r3/reviewer/findings.md`.

## What the source fixes achieved

| Earlier issue | Fresh result |
| --- | --- |
| Text over important project details | Builder caught its initial wide-screen collision during its own QA and corrected it before delivery. Final five-size review shows no copy covering the room. |
| Fine-print mobile proof | Trust text is readable and reflows on mobile instead of preserving a compressed desktop row. |
| Missing source typography samples | Helpers recovered animated heading and below-fold prose. All ten heading/body comparisons match actual Reem Kufi/Poppins glyphs; no font-availability warning. |
| Desktop validation loses label/summary | Summary now receives focus with scroll spacing. Parent verified both direct-submit and CTA-first paths at mobile, short laptop and desktop. Error links reveal the full field group. |

The first three visual/QA corrections were source changes before attempt 2; the validation-context clarification was added before attempt 3. This accepted result came from that frozen skill without parent design coaching or code edits.

## Separate carryover result

| ID | Status in business 2 |
| --- | --- |
| C01 | Inline pending-state/snapshot checks pass; one request, disabled edits, late aborted result ignored. Exact modal-close/reopen variant not exercised. |
| C02 | Pass: confirmed state survives CTA return and even rejects a repeated submit event; deliberate New enquiry resets. |
| C03 | Pass: actual 8-second deadline, uncertainty, preserved fields, no auto retry, successful same-filled deliberate retry. |
| C04 | Pass for current hero and all five required sizes; no action/proof collision. Original team-image variant absent. |
| C05 | Error-link keyboard focus passes. Required grouped field absent, so that exact case remains N/A. |
| C06 | Pass: mixed alphabetic/digit phone junk rejected; international number plus extension accepted in its validated representation. |
| C07 | Pass: plain preview confirmation, compact panel, no tall empty shell. |
| C08 | Pass: mobile hero and every below-fold photo use optimized responsive derivatives, not full original downloads. |
| C09 | Current essential room/glazing remains inspectable. Short-laptop crop is explicitly accepted below, not silently cleared by a global pass. |
| C10 | All placements decode at five widths; no missing-image finding. Separate delayed/broken helper fixtures remain recorded with the source correction. |
| C11 | Final parent reproduction and fresh reviewer both pass their actual scope. No claim that this retroactively accepts Stayclean or proves absent modal/group variants. |

Parent evidence: `parent/parent-checks.json` contains **30 passing assertions**: 13 lifecycle/carryover checks, 12 validation-context checks across three widths and two starting positions, and five per-viewport image-load checks. Requests were intercepted and synthetic. Screenshots include `parent/desktop-validation.png` and `parent/success.png`.

## Quality evidence

- Static/surface scans pass. Browser measurement: zero failures, two named hero-crop warnings. Fonts match at all five required viewports.
- Builder: 18 synthetic conversion assertions, plus five viewport measurements. Parent: 30 assertions. Fresh reviewer: five first screens/full pages, readable sections, and invalid/failure/success states at all five widths.
- Final Lighthouse 12.8.2: **Performance 99, Accessibility 100, LCP 2180ms, CLS 0, TBT 0**. The final run selected performance/accessibility categories; do not invent final SEO/Best Practices scores. Earlier progress mentioned 24ms TBT; the saved final JSON is authoritative at 0ms.
- The builder independently found and corrected its initial image/text collision and small link targets. Its first Lighthouse result was 90 with 3.68s LCP; responsive image delivery and subsequent work improved the saved final result. One stalled repeat was bounded and retried; no unavailable-tool excuse replaced the audit.

## Accepted limits and remaining work

- At 1280x600 the shallow hero crop omits decking and the lower glazing. The roof, room and upper glazed front remain clear, with complete project structures elsewhere. Parent and fresh reviewer agree this is discretionary polish, not hidden proof or a blocker. The 1440x900 crop warning is also explicitly reviewed. Crop percentage alone is not a defect verdict.
- Element-only captures can show an off-screen skip link during stitching. Ordinary viewport captures and position checks confirm it is hidden until focused. No page repair was made for that capture artifact.
- Form confirmation is preview-only and input is discarded. Server-side persistence/deduplication, CRM, analytics, production privacy wording, image publication permission, Cloudflare and physical-device/live testing remain outside this phase.
- Stayclean is still unaccepted. Exact modal/group carryover branches remain open. Business 3's image-generation run has not started. This result supports the current external-reference case, not a universal guarantee of perfect pages.

## Handoff

Accepted page and raw build evidence: `evidence/external-reference-r3/project/`. Start its local receiver with `PORT=4175 node server.mjs` from that directory, choosing a free port if necessary. Only publish static page files after the later authorized connection work; never expose its server, build reports or QA tools as site assets. Parent restarted this same unchanged preview server after closing the completed agents.

Source and all three attempt audits are on the separate `community/pro-review-handoff-20260918` branch. Luke's original README-BOHDAN.md remains unchanged. Stop rerunning this business now that bounded local acceptance passed; do not consume the remaining allowance simply to reach 50%.
