# Clarentis attempt 7 acceptance

2026-09-20. Frozen source 56a6cb4; all 175 source files unchanged after execution. Independent builder Godel and fresh reviewer Mencius: Sol 5.6 xhigh, fork_context=false. No prior audits, builds or parent context supplied. Shared-host filesystem boundaries are instructed, not OS isolation.

## Verdict

Image-generation and parent technical checks pass. Overall clean sign-off remains withheld for two narrow-screen/editorial warnings. No severe visual defect was found. Do not call this the perfect skill or retroactively accept the other businesses.

1. Header phone is 11px at 320px and 12px at 390px. The build reduced it to fit logo/number/menu on one row. Existing guidance required a readable phone but lacked a minimum for that specific contact cue. build-contract.md now sets a 14px baseline and tells the builder to reflow the header; quality-gates.md includes the computed size in the existing first-screen review. No new viewport or review round.
2. Final FAQ says 'Clarentis lists a free initial consultation'. This is less severe than the earlier explicit source-service-list framing, which is gone, but still indirect. The copy reference now tells FAQs to answer directly before context. Do not broaden a regex into a blacklist of every ordinary verb; 'lists' has legitimate uses. Existing narrow validator still passes correctly for the pattern it actually tests. A human/agent editorial judgment remains necessary.

## What improved and what is proven

- Four distinct content images, each used once, with service-relevant subjects and visible disclosure. Reviewer inspected all originals and desktop/mobile placements. Four actual native image calls used generatedImage, with no full-result JSON serialization. Retained originals match tool-returned hashes. Exact prompts and local original paths are portable. Tool-managed returned filenames are absent from the builder's image plan; parent provenance record supplies the mapping, so do not claim the builder recorded that field itself.
- 107 parent assertions pass against the file-openable publish package: byte parity for HTML/CSS/all published assets, intended contact URLs, keyboard FAQ activation, phone visibility, image identity/decoding/captions, overflow and local resource loads. No emails or calls dispatched.
- New static copy check passes with no research_voice_copy match; no missing local assets, dead links or image-contract failures.
- Ten matching source/applied rendered-font pairs; final page does not load the initially considered alternative font. Lighthouse mobile Performance 100, Accessibility 100, LCP 1503.4402ms, CLS 0, TBT 0. Only those two categories were measured in this report.
- Automated short-height crop warning is accepted from actual pixels: the relevant desk objects and caption remain visible. No repeat photo, face obstruction or incoherent layout overlap found.
- Reviewer HTTP connection was unavailable, so it inspected the actual file package; this is not a hosting-routing pass. Production delivery, CRM, tracking, Cloudflare and PDF modules remain untested. No modal exists here, so its error/exit behavior is not cleared.

## Next bounded test

Apply both agreed source corrections before freezing the next build. Use the remaining allowance to revisit the form-led Garden Room external-reference case, carrying these two items separately alongside the unresolved modal-close and source offer/process checks. Keep Clarentis page unchanged as evidence. Current usage 66% used; stop at 70% used and preserve partial work if that boundary arrives. All new launches remain sequential, context-free and uncoached.

Evidence: evidence/image-generation-r7/project, parent-audit and independent-review. Full reviewer report and screenshots are also in outputs/clarentis-r7-visual-review/ in the parent workspace.
