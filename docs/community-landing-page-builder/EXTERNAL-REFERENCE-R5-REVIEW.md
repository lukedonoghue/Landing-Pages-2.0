# Garden Room attempt 5: parent acceptance

Reviewed 2026-09-20. **Blocked for full product acceptance; technical interaction checks pass.** User explicitly authorized carrying the remaining source corrections forward to case 3 instead of another Garden Room rebuild. No generated page was manually repaired.

## Independent build

Galileo, agent `01a0bf4a-f9a7-7d10-b79c-054da237bb8c`, Sol 5.6 xhigh, `fork_context=false`, source 6bc6076. The agent received the same plain owner request, frozen skill, business/reference URLs and empty project as prior attempts, with only run paths changed. It received no earlier findings or conversation. Frozen skill before/after: 175 files, SHA-256 `a393d5d43128c2657f65ec413cfabf2891e2ac23a328545360d2479f91c7271f`. Isolation is instructed filesystem scope on a shared host, not an OS sandbox or new account. Completed builder is closed.

Fresh reviewer Turing, `01a0bf6a-4589-7672-a3c2-c35a10ae5b70`, Sol 5.6 xhigh, `fork_context=false`, reviewed current artifacts and public sources without prior findings. Its raw report is preserved under `evidence/external-reference-r5/reviewer/review.md`. Parent calibrated the findings below instead of automatically accepting the review verdict or its proposed changes. Reviewer is closed.

Preview: http://127.0.0.1:8787/ . Portable artifact is `evidence/external-reference-r5/project/`; start its adjacent `work/preview_server.py` for the synthetic form receiver. The earlier port 4176 serves attempt 4, not this build.

## Findings and source corrections

| ID | Finding and evidence | Real cause and disposition |
| --- | --- | --- |
| R5-P01 / P2 | `project/index.html:152-162` promises a finished-room process but stops at reviewing a quote. The official About page supports dedicated project management after commitment, and FAQ supports qualified timing. | The generic process/coverage instruction was satisfied by a pre-sale list rather than the heading's actual promise. `copy-and-structure.md` now ties process scope to its stated endpoint and requires relevant delivery/timing explanation. No fabricated timetable or extra section quota. |
| R5-P02 / P2 | `project/index.html:178` names the price promise but omits what it does. Saved official `work/public-source/contact.html` explicitly promises to beat a lower like-for-like quote. The research brief already captured this. | Evidence was discovered but lost during cautious copy compression. Existing copy rules now require the concrete benefit and conditions together, or a reasoned omission. Another research step would not solve this. |
| R5-P03 / P3 | Geographic fit exists only in closed FAQ `index.html:207`, with optional location at 253. | The prior rule said visible copy without specifying early qualification. `reference-fidelity.md` now asks for an early qualified cue, not a giant location list, new mandatory form field or unsupported coverage guarantee. This is weaker discoverability, not missing geography or a broken form. |
| R5-P04 / process gap | Builder `work/research_browser.py:50-60` and `qa_browser.py:45-46` checked computed families/font resources, not glyph fonts; no helper-compatible `build/brand.json`. Parent's actual source/applied glyph check passes. | The main workflow did not directly route the bundled helper while the deeper design reference did. `SKILL.md` now names the existing extraction/report reuse at the point of use and defines equivalent evidence for custom tooling. No new check, service or API key. |

Reviewer finding about another editorial heading being below the first fold is **not accepted as a blocking rule**. The 390, 1024 and 1280 captures expose readable substantive trust content after the hero, satisfying the existing continuation requirement. `quality-gates.md` now makes this explicit to prevent progressive shrinking to fit two following sections. At 320x568 the photo is only 90px tall and reads as a narrow strip; record that quality caveat and prefer protecting media scale, not further compression. The current page remains blocked on P01/P02 regardless.

No wholesale visual redesign is indicated. Parent and reviewer see readable type, clear public phone, unobscured glazing at primary viewports, coherent current color treatment and materially richer decision content. Missing competitor-specific showrooms, finance or product ranges is correct, not a reason to fabricate them.

## Verification performed

- Parent `parent/parent-checks.mjs` and JSON: **81 assertions pass** across 320x568, 390x844, 768x1024, 1024x800, 1280x600, 1440x900. Every visible enquiry placement opens the same modal without a page/hash jump, Escape restores focus, phone remains visible, ordinary Tab focus remains visible/contained, validation summary and correction links work, all placed images decode, and no horizontal overflow is measured.
- Synthetic interaction: whitespace, malformed email and alphabetic phone rejected; international extension accepted; pending duplicate guarded; close/reopen locks snapshot; receipt while closed persists; confirmed duplicate guarded; deliberate reset; 503 recovery preserving values; real local receiver retry; mismatched request receipt rejected; actual eight-second timeout and subsequent retry pass. No live lead sent or stored.
- C01/C02/C03/C06 verified for actual modal. C05 ordinary-field cases pass; required radio/group case and shared inline/modal variants remain **not exercised**, because those variants do not exist here. C07 compact plain-language result passes. C08 responsive WebP derivatives at all photo placements and C10 decoded-image checks pass. C04/C09 primary-size subject protection improves; retain narrow 320px crop caveat. C11 correctly withholds overall acceptance despite technical passes.
- Parent bundled browser helper: zero failures, seven warnings. Actual heading/body glyph comparisons match the official source at all five standard viewports: Reem Kufi and Poppins. No formal brand guide is claimed. Evidence: `parent/source-brand.json`, `parent/browser-review.json`.
- Crop warnings are the same hero at 768 and 1024. Readable captures show the building opening unobstructed; surroundings are cropped. The percentage alone is not a defect. Small-link warnings were inspected: inline caption/source links and spaced footer links; header phone/final phone also have small text boxes but separated hit areas. Not counted as a demonstrated inaccessible target. Larger padding remains a polish opportunity, not a new blocking sweep.
- Parent static validator and prohibited-surface scan pass with no findings. Saved final Lighthouse 12.8.2 raw report: performance 98, accessibility 100, LCP 2480.698ms, CLS 0.00002082, TBT 0. Earlier three saved performance runs are also 98 and approximately 2.48 seconds. Parent verified the raw records; did not claim to have rerun Lighthouse.
- Independent review covers all six first screens, readable lower sections, source/reference comparison and modal pixels. Public source confirms the real projects, attributed excerpts, phone and company details. Original first-party media retained with URL mapping; republication rights remain unresolved.

## Limits and next case

This is page-only acceptance, not CRM, tracking/provider, physical-device, clean-machine, Cloudflare or real lead-delivery acceptance. The receiver does not persist/forward details. Original Luke README remains unchanged against 1693490. Stayclean is not retroactively accepted.

The user raised the stop to **70% weekly used / 30% remaining**. Finish source correction, then start Clarentis image-generation case from another empty project and fresh agent. Carry P01-P04 separately into its parent audit when applicable; never give this audit or a bespoke repair brief to the builder. Garden Room remains blocked and unchanged, even if Clarentis passes.
