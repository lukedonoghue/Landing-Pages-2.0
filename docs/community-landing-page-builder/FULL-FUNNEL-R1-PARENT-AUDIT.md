# Clarentis Full-Funnel Run 1: Parent Audit

Date: 2026-09-21. Independent builder: gpt-5.6-sol xhigh, no forked conversation, frozen source 8034d88. This is business 3, not Stayclean. A later parent follow-up asks for helper-change documentation and read-only release readiness; it is explicitly post-build review, not part of the unaided first pass.

## Verdict

Update at 09:20 UTC: the assisted continuation now has all nine local revision-bound gates recorded without failures, with explicit demo warnings. The supported-runtime regression passed 200/200 tests. The selected connected Cloudflare account and disposable synthetic-contact scope are authorized; remote deployment is still pending confirmation. The earlier findings below describe the initial independent run and must not be mistaken for the current assisted state.

The local Worker/D1 CRM journey is evidenced. The page is not fully visually accepted, not deployed, and not proven publish-ready. GTM import and enhanced-conversion delivery remain pending real destination inputs. Do not turn the builder's automated passes into an end-to-end success claim.

The user explicitly requested continuing this demo through CRM/deployment while saving the new PDF/default and main-site-exit corrections for the next fresh build. Those changes do not retroactively clear this run.

## Confirmed Local Evidence

- Local page: http://127.0.0.1:8899/ . Title identifies Clarentis; /api/health reports a connected database.
- Four genuinely distinct generated accounting-related content images. Parent inspected the actual assets and their desktop/mobile placements. They are labelled illustrative, not presented as employee/customer proof.
- Actual source/applied glyph fonts match in the browser evidence. Phone digits are readable near the top.
- Chromium/WebKit interaction report passes. The builder's nine-size layout report passes with warnings. Parent captures cover 320x700, 390x844, 768x1024, 1024x800, 1280x600 and 1440x900, before and after declining optional data, plus full desktop/mobile sections.
- Lighthouse report: performance 100, LCP 1599ms, CLS 0, TBT 0. These are local laboratory results, not deployed performance.
- Final local journey reports named login, accepted public form, thank-you navigation, correlated stored receipt, persisted stage/note changes, source/device reporting and revoked session after logout.
- Receipt: 44131f59-a077-4f0b-ade2-7e259ce66db2. Lead: bd31e7f9-4fff-465c-be85-54c11ba2dacc. Stored receipt matches. Evidence source is authenticated Worker API backed by local D1, not a separate direct SQL inspection.
- The brochure assertion is marked not applicable (`brochure_applicable: false`). There is no PDF. Do not cite the boolean journey field as a successful download.

## Unresolved Findings

| Priority | Finding and evidence | Cause and next-run correction |
| --- | --- | --- |
| P1 readiness | Complete guarded release readiness is not established. A Markdown copy master and capture exist, but the full Worker workflow expects structured copy and additional revision-bound gates. | Main skill allowed either Markdown or JSON without routing complete CRM/publishing builds early to the structured contract. Canonical SKILL.md now explicitly routes these builds to JSON and copy-workflow.md. Parent requested actual status/preflight evidence, not invented gate reports. |
| P2 | Footer logo is a solid white rectangle at desktop and mobile. | public/styles.css:157 applies brightness(0) invert(1) to the opaque logo. A loaded image is not a visible logo. Design guidance now names this failure and requires reviewing each distinct logo treatment. Current generated page is unchanged. |
| P2 | At 320x700, the undecided-consent panel covers the main action; after declining, the action is visible but the hero photo starts at y714 and the following section at y830. The photo is reduced to 116px high. | Hero wraps and fixed consent combine; the narrow spot's phone check did not establish complete first-screen quality. Existing QA instructions now require inspecting both privacy states and explicitly reject treating the phone check as full acceptance. |
| P2 | Desktop hero supporting copy and price qualifiers cross the detailed table/receipt portion of the photograph, with uneven visual contrast. | Hero copy width and background crop were checked geometrically but not adequately against the actual pixels under all lines. Existing photo-backed text review now expressly includes supporting lines and qualifiers, not just the headline. No numeric contrast result is claimed here. |
| P2 next policy | No brochure/PDF. | Frozen instructions explicitly made it optional. Canonical source now defaults to a useful researched PDF, allows only a source-supported buyer rationale for omission, and extends the existing static check. Not applied retrospectively. |
| P2 next policy | Footer sends visitors to https://clarentis.co.uk/ . | Frozen reference/build rules allowed external research destinations. Canonical skill now prohibits main-site/subdomain exits across visitor surfaces, with a source-host static check and browser/PDF review. Not applied retrospectively. |
| P3 copy | MTD section says Clarentis "lists" services and FAQ introduction refers to the "published starting point". | Research/reporting voice persists despite the existing direct-copy requirement; the narrow phrase detector is not semantic copy review. Record for next acceptance rather than adding an ever-growing phrase blacklist. |

The initial browser pass also caught focus reaching the offscreen spam field, obscured short-height modal controls and oversized heroes. The builder corrected those before its later pass. Those early failures are not silently discarded, but are distinguished from remaining parent findings.

## External Inputs and Remaining Acceptance

- User must select the intended Cloudflare account. The historical Byrider account is not silently reused.
- Public demo needs a real operator name and privacy-contact address, without pretending the real business operates this test.
- GTM/Ads IDs and conversion label are missing. No fake import-ready container and no live enhanced-conversion success are claimed. An isolated advertising test destination is separate from production; synthetic tests must not pollute a production account.
- Actual provisioning, remote migrations, guarded release identity, public CRM access and live synthetic receipt/stage/note/reporting checks are still required.
- Physical-device testing, remote restore/cutover and production Ads diagnostics have not been performed.

## Source and Evidence Separation

Current page remains unchanged by the parent. Corrections are in the canonical community skill for the next fresh run. Frozen run instructions remain unchanged. Generated helper adaptations are being reviewed separately and must not be promoted to the skill merely because they made a local test pass. All source updates and review evidence go to the separate community branch; Luke's original README and skill are untouched.

## Confirmed Integration Defects and Recovery

The read-only follow-up confirmed the formal workflow still reported `research`, with no gate manifest and nine required handoff gates missing. JSON copy master, client brief, research context and editorial review were absent. This is an instruction/integration failure, not merely a missing user credential. Local reports existed but were not enough for a guarded release.

Two deterministic source defects were also reproduced and fixed in the canonical community package:

- Scaffold copied font-dependent measurement helpers but omitted `rendered_fonts.mjs`. It now includes the unchanged helper, with a fresh-scaffold regression.
- Preflight treated every occurrence of `Your business` as starter content, including a legitimate form legend. It now checks the actual starter brand treatment, unchanged strong development-template markers and the configured site-name check. A focused regression proves the normal legend passes while default branding/template text still fails, with zero remote calls.

Canonical tests after these changes: 76 Python package tests pass; the focused Node publishing-preflight regression passes. The generated project's 193-test run is separate evidence, not a claim that the new canonical source has passed a new independent full build.

At 07:59 UTC the parent requested a coached recovery to assemble genuine missing local workflow evidence without page redesign or remote writes. The parent supplied its actual remaining visual findings and prohibited claiming them passed. Any resulting continuation is intervention-assisted, not the untouched first-run acceptance. The original frozen skill and page are preserved. Account selection and privacy/Ads details remain unresolved.

## Assisted CRM Integration, 09:20 UTC

The user subsequently authorized the connected Cloudflare account and synthetic disposable-demo contacts. The frozen skill remains unchanged. The generated project received the documented canonical attribution/UI fixes and minimal consent/copy repairs, so the earlier statement that its page is entirely unchanged no longer applies to this assisted continuation. GTM destination IDs and an owned custom domain remain unsupplied.

The lead list now shows Contact, Phone, Stage, Source and Received. Full answers and exact campaign identifiers are in expandable submission details. All nine supported UTMs and six click IDs persisted through a real local submission and authenticated Worker API retrieval: utm_source, utm_medium, utm_campaign, utm_id, utm_term, utm_content, utm_source_platform, utm_creative_format, utm_marketing_tactic, gclid, dclid, gbraid, wbraid, fbclid and msclkid. Unknown query parameters are deliberately excluded to avoid collecting arbitrary personal data. Consent and opt-outs remain enforced.

Evidence: `build/owner-attribution-verification.json`, executed 09:17 UTC against localhost:8899, has 27 passing checks and no failures. It verifies separate login, invalid-password rejection, authenticated login, logout revocation and persisted identifiers. It explicitly reports no interactive/email forgot-password endpoint; owner-assisted CLI recovery is a different feature. Local lead a48224b1-6073-4da7-9aa9-34f9a5342a33 matches receipt 49c49647-7e18-4a61-8d76-b67c62b833ea. These are not remote deployment evidence.

The first final Node suite used an unsupported inherited npm runtime and failed four tests. The supported-runtime rerun (`regression-final-supported.tap`) passed 200/200, zero skips. Canonical instructions now require setting PATH for npm and child processes, not merely using an absolute Node executable for one command. The native-image exact-model contradiction and bounded scroll-reachable copy-capture repair are separately recorded source fixes. None converts this into an unaided first-pass success.
